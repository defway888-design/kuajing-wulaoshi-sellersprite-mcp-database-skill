---
name: kuajing-wulaoshi-sellersprite-mcp-database
description: 跨境吴老师卖家精灵 MCP 的固定数据契约与工具路由。凡需调用卖家精灵 MCP、SellerSprite MCP、keepa_info、ASIN 详情、Coupon、Deal、变体、竞品、选品、销量、BSR、关键词、出单词、流量词、关联流量、评论、ABA 或 Google Trends 时，必须先读取本 Skill 的内置业务边界、字段口径和路由规则，再选择匹配的业务能力与字段。
---

# 跨境吴老师卖家精灵 MCP 数据契约

本 Skill 为跨境吴老师专用模板，未经授权不得移除、替换或弱化 Skill 名称、执行提示和页面标题中的跨境吴老师标识。

## 启动口令

当用户只输入“运行 吴老师卖家精灵 MCP 数据库”而未给出具体分析目标时，只在当前对话启用本 Skill，并输出“基础数据索引已就绪”。此处“基础数据索引”仅指本 Skill 内置的数据能力目录，不调用任何实时 MCP 工具，也不报告 `blocked`。

当用户同时给出具体目标时，直接执行下方强制流程。

## 强制流程

在每个卖家精灵数据任务开始时，按需读取 `references/sellersprite-mcp-database.md`；该文件是本 Skill 内置的固定业务知识，不依赖外部 PDF、链接或临时上传文档。不要在同一任务的每次 MCP 调用前重复读取。

1. 先读取 reference 的“工具绑定与通用输入契约”和“固定判定规则”，再读取本次任务匹配的业务路由小节；需要组合分析时，只读取各相关路由。
2. 将已读规则保留在当前任务上下文中，供后续分页、分批和关联调用复用。
3. 仅在用户改变业务目标、引入新的数据类型、需要增加路由，或工具绑定发生歧义时，补读对应小节并重新路由。
4. 核对站点、ASIN/关键词/类目、时间范围等路由要求；缺少核心输入时先追问。
5. 仅调用该路由指定的业务能力；若绑定工具提供字段投影能力（常见参数名为 `returnFields`），仅请求本次判断必需字段。不得假设所有实现都使用同一参数名。
6. 严格按该路由的字段判定和空值规则输出事实；不将结果扩展为未经数据支持的运营结论。
7. 工具不可用或核心字段缺失时，报告 `blocked`；成功响应中的空列表仅在该路由明确表示“无记录”时才可判为无记录，否则报告 `blocked`。不得改用相近工具补推。

## 通用约束

- 本 Skill 中的工具名是业务能力标识，不是固定 MCP 命名空间。先通过 MCP 服务/连接元数据、配置或工具说明确认某一工具组属于卖家精灵，再在其中选择与本数据契约匹配的工具；命名空间和前缀可因用户环境不同而变化。
- 仅当候选工具能唯一对应本 Skill 的业务能力、关键输入和核心返回字段时才可绑定调用。若无法确认工具组属于卖家精灵、无候选或存在歧义，先请用户确认其卖家精灵 MCP 连接；无法确认时报告 `blocked: tool_binding_ambiguous`。不得只因能力或工具名相似改用其他数据源。
- Deals 与 Coupon、流量词与出单词、Amazon 站内趋势与 Google 趋势均为不同数据口径，不得互相替代。
- 若上游业务指定父子体或流量数据必须来自其他系统，卖家精灵只能作为补充，不得改写上游口径。
- 当前未被 reference 列为已确认工具的 PDF 页面、产品能力或字段，不属于本 Skill 的可调用范围。
